Graph :
    traversal = dfs, bfs
        visit/process thayelo node fari aave to...
    shortest path = bellmen ford, dijkstra, floyd-warshall
    mst = kruskal, prims
    directed acyclic = topological with dp
    Floyd = slow, fast, meet->clycle
    MultiSource bfs

Tree :
    Arbitrary root a tree
    Recursion (each children = sub tree(sub problem))
    traversal = dfs
        use with dp
    binary - in, morris, pre, post
    pre - change val when going down
    post - take some imp val from child
    pre + post
    bst hoy to sorted inorder

Sorted hoy to :
    binary search

Dynamic Programming :
    0 thi i no jawab = min/max(0 thi j no jawab + c)    j<i;
    i thi j no jawab = min/max(i thi k + k thi j + c)   i<k<j;
    upar ane left vada + corner dir vada block no jawab

Stack :
    use another stack

Array :
    left sum, right sum
    0 thi n-1 ma hoy to shuffle ke swap karine.
    koi sorting method nu core function lage to, merge-select-insert procedure
    index wise store karo, enathi index ane value bei mali jase.
    Two pointer

Common :
    time complexity nakki kar
    Sorting vapri juo.
    Greedy lagadi ne examples check karo.
    Direct math formula
    Bit wise try karo
    Divide and Conquor vapri juo, ke sol efficiently merge thay chhe?
    recurrance relation banavo, problem size ne reduce karine.
    Ka to dp
    ka to backtrack
    reverse karine check kari juo
    javab par binary search
space bachavva input ma j set karo.
nana nana udaharan thi start karo.

Query :
    offline / online
    prefix/suffix
    darek mate log n val save karine table banavo, javab mate target ne log n ma decompos karo
    segment tree
    trie
    traversal array - sub tree = sub array

string :
    hash val ghani var kam lagi ske
    nakama char ne - banavi do, ka to ee char ni jagya e kamna char muko
    Two pointer

Link List:
    base case nai bhulvana.

Setting :
    tree diameter - take a node, find farthest from it, that is one end, again find farthest from that end, that is second end.
    spiral k zigzag hoy to 2 stack.
